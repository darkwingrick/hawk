# ToDos:

## Needs to Have
[X] Add a spinning circle or some type of animation to a workspace when the terminal inside that workspace is active.  
[X] Make the select in the active workspace panel a little more highlighted.  
[] Replace current auto-complete with the ability to add model.  
[X] When the terminal is creating other tabs, it's not doing that well. The focus is always on the most recent tab versus the tab next to where the plus is.  
[X] Inside the terminal, I need to be able to move the cursor around with my mouse and not just the arrow keys.   
[] Count the number of files that are changed in git and put that number as a notification over the git icon.   
[] Claude Code integration  
[] Codex integration  
[X] Update toolbar to have the same color as the highlighed Active Workspace
[] Format XML

## Nice to Have
[] Create Windows release  
[] Update the markdown templates so that it's more viewable, and make it configurable so they can change it.
[] Port to Windows
