# ToDos:

## Needs to Have
[-] Add a spinning circle or some type of animation to a workspace when the terminal inside that workspace is active.  
[-] Make the select in the active workspace panel a little more highlighted.  
[] Replace current auto-complete with the ability to add model.  
[] When the terminal is creating other tabs, it's not doing that well. The focus is always on the most recent tab versus the tab next to where the plus is.  
[] Inside the terminal, I need to be able to move the cursor around with my mouse and not just the arrow keys.   
[] Claude Code integration  
[] Codex integration  


## Nice to Have
[] Create Windows release  
[] Update the markdown templates so that it's more viewable, and make it configurable so they can change it.
